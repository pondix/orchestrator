# Scripts

Utility scripts for development and operations.

## Planned helpers
- `dev-start.sh`: start backend + frontend in dev mode.
- `seed-demo.sh`: prepare demo data and topology for the UI.
- `lint.sh`: run Go + frontend linting.
- `test.sh`: run unit and integration tests.
